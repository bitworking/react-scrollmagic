import { Controller } from './';
import { Scene } from './';

describe('Controller', () => {
  it('is truthy', () => {
    expect(Controller).toBeTruthy()
  })
});

describe('Scene', () => {
  it('is truthy', () => {
    expect(Scene).toBeTruthy()
  })
});
